import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.academictracker',
  appName: 'Academic Tracker',
  webDir: 'build'
};

export default config;
